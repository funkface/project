<table>
    <thead>
        <tr>
            <?php foreach($this->columns as $column => $sort): ?>
            <th class="<?php echo $sort['class']; ?>"><a href="<?php echo $sort['url'] 
            ?>"><?php echo $column; ?></a></th>
            <?php endforeach; ?>
        </tr>
    </thead>
    <tbody>
        <?php foreach($this->rows as $row): ?>
        <tr>
            <?php foreach($this->columns as $column => $sort): ?>
            <td><?php echo $this->escape($row[$column]); ?></td>
            <?php endforeach; ?>
        </tr>
        <?php endforeach; ?>
    </tbody>
</table>

<div id="pager">Page: <ol><?php 
$this->pagerControls->setTemplate('<li><a href="{%url}">{%page}</a></li>');
$this->pagerControls->display();
?></ol></div>